import { TableObserveDirective } from './table-observe.directive';

describe('TableObserveDirective', () => {
  it('should create an instance', () => {
    const directive = new TableObserveDirective();
    expect(directive).toBeTruthy();
  });
});
